namespace ColonyBuildingSim.Inventory
{
    /// <summary>
    /// Defines colony-level resource types that can be stored, spent, and queried by colony systems.
    /// </summary>
    public enum ResourceType
    {
        Wood = 0,
        Stone = 1,
        Food = 2,
        Meal = 3
    }
}